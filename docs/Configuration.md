# Configuration

Runtime configuration examples live in `source/config`.

The initial scaffold defines the default service identity and bind address in Rust code. File-backed configuration should be introduced with a typed parser and tests before production deployment.
