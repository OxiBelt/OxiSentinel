# Kernel Extension

This directory is reserved for system-level installation and verification assets.

The scaffold intentionally contains placeholders only. Add platform-specific scripts after the runtime requirements are defined and tested.
