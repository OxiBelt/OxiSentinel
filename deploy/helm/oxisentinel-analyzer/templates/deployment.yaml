apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "oxisentinel-analyzer.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "oxisentinel-analyzer.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "oxisentinel-analyzer.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "oxisentinel-analyzer.name" . }}
    spec:
      serviceAccountName: {{ default (include "oxisentinel-analyzer.fullname" .) .Values.serviceAccount.name }}
      containers:
        - name: analyzer
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
