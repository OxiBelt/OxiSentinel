apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "oxisentinel-gateway-controller.fullname" . }}
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "oxisentinel-gateway-controller.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ default (include "oxisentinel-gateway-controller.fullname" .) .Values.serviceAccount.name }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "oxisentinel-gateway-controller.fullname" . }}
